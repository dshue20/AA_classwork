extends ../layout.pug
include ../utils.pug

block content 
  if errors 
    +errorsField(errors)
  form(action='/park/add', method='POST')
    include park-form-fields.pug 
    button(type='submit') Add Park
    a(href='/') Cancel